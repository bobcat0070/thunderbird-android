<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="8dp"
    >

    <RadioGroup
        android:id="@+id/classify_options"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        >

        <RadioButton
            android:id="@+id/classify_human"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/classify_as_human"
            />

        <RadioButton
            android:id="@+id/classify_notification"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/classify_as_notification"
            />

        <RadioButton
            android:id="@+id/classify_newsletter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/classify_as_newsletter"
            />
    </RadioGroup>

    <!--
        Bulk senders rotate the local part of their address, so correcting one message would otherwise never
        match the next one from the same sender.
    -->
    <CheckBox
        android:id="@+id/classify_whole_domain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        />
</LinearLayout>
